export class Point {
  id: string;
  name: string;
  lat: string;
  lon: string;

  constructor(id: string, name: string, lat: string, lon: string) {
    this.id = id;
    this.name = name;
    this.lat = lat;
    this.lon = lon;
  }
}
